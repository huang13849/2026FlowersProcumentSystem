apiVersion: apps/v1
kind: Deployment
metadata:
  name: scene-service
  namespace: supply-chain
  labels:
    app: scene-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scene-service
  template:
    metadata:
      labels:
        app: scene-service
    spec:
      nodeSelector:
        kubernetes.io/hostname: huangfra-ubun-master-ms-7e11
      containers:
      - name: scene-service
        image: 100.76.15.64:5001/supply-chain/scene-service:latest
        imagePullPolicy: IfNotPresent
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3012
        envFrom:
        - configMapRef:
            name: supply-chain-common
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3012"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 1000m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3012
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3012
          initialDelaySeconds: 3
          periodSeconds: 10
