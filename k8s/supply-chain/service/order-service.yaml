apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: supply-chain
  labels:
    app: order-service
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
  - port: 3008
    targetPort: 3008
    nodePort: 31008
    protocol: TCP
    name: http
