apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: supply-chain
  labels:
    app: product-service
spec:
  type: NodePort
  selector:
    app: product-service
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 31099
    protocol: TCP
    name: http
