apiVersion: v1
kind: Service
metadata:
  name: scene-service
  namespace: supply-chain
  labels:
    app: scene-service
spec:
  type: NodePort
  selector:
    app: scene-service
  ports:
  - port: 3012
    targetPort: 3012
    nodePort: 31012
    protocol: TCP
    name: http
