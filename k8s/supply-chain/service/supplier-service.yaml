apiVersion: v1
kind: Service
metadata:
  name: supplier-service
  namespace: supply-chain
  labels:
    app: supplier-service
spec:
  type: NodePort
  selector:
    app: supplier-service
  ports:
  - port: 3002
    targetPort: 3002
    nodePort: 31002
    protocol: TCP
    name: http
