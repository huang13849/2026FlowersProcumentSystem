apiVersion: v1
kind: Service
metadata:
  name: shop-service
  namespace: supply-chain
  labels:
    app: shop-service
spec:
  type: NodePort
  selector:
    app: shop-service
  ports:
  - port: 3004
    targetPort: 3004
    nodePort: 31004
    protocol: TCP
    name: http
